import { create } from '@biorate/symbolic';

/** @description Run-context symbols container. */
const Symbols = create('Context');

/** @description Scenario metadata symbol. */
export const Scenario = Symbols.Scenario;

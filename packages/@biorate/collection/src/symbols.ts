import { create } from '@biorate/symbolic';

/**
 * @description Symbolic key for storing types metadata.
 */
export const Types = create('Types');
/**
 * @description Symbolic key for storing properties metadata.
 */
export const Props = create('Props');

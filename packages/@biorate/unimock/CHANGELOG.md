# Changelog

## 1.0.0

- Initial release: `@Mockable()` decorator, snapshot record/replay proxy, env flags.
- Default snapshot dir: `__snapshots__`, built-in `defaultSerializers` for opaque SDK handles.
